const SOLT_ROUND = 10;
const JWT_NOT_SECRET = 'Not-so-secret';
const DB_LOCAL_CONNECTION_STRING = 'mongodb://localhost:27017/moviesdb';

module.exports = {
  SOLT_ROUND,
  JWT_NOT_SECRET,
  DB_LOCAL_CONNECTION_STRING,
};
